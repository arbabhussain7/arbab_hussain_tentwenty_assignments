import 'package:flutter/material.dart';

class MediaLiberyScreen extends StatelessWidget {
  const MediaLiberyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Media Library Screen')));
  }
}
